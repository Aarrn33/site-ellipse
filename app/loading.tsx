/**
 * Contenu affiché par défaut lors du chargement d'une page
 */

import PageTitle from '../components/pageTitle/pageTitle';

export default function Loading() {
  return <PageTitle>Chargement...</PageTitle>;
}
